# Browser CDP internals

Vendored from the modular browser example shipped by `@howaboua/pi-codex-conversion` 3.0.23. Keep this directory synchronized with that example as a unit; `cdp.mjs` is the stable entry point and `launcher.mjs` contains the environment-specific browser startup policy.
