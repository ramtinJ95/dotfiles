Global rules:
- If scanned code looks off, add `./scratch/BACKLOG.md` entry with file/lines.
- Use ctx7 (`npx ctx7 ...`) for relevant current docs.
- When choices matter, compare options and ask before implementing; ask if unclear.
- Prefer descriptive names; avoid obvious comments.
- Use parallel tool calls for independent work.
- PRs: concise, no checkboxes/generated-by.
- Commits: small, conventional; never add Co-Authored-By.

