# Files opened from Finder or `open` use Neovim in a new Kitty window.
protocol file
ext md,markdown,txt,text,json,yaml,yml,log,csv
action launch --type=os-window -- /opt/homebrew/bin/nvim -- $FILE_PATH
